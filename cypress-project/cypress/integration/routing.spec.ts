import { describeAllPages } from "cypress/support/routing"

describe("Creation of ToDo's", () => {
    describeAllPages(() => {
        it(`do your test`, () => {
            // ...
        })
    })
})
